# i18n/

Qt `.ts` translation files generated from the web app's six locales
(en, de, es, fr, ja, zh) by a `tools/` script in Phase 4, with a key parity
check against `src/lib/locales/*.ts` upstream. Empty in Phase 0.
